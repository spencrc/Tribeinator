# Folders that will get installed when building the Docker container.
/node_modules
# Folders and files that always exist, but shouldn't be in our container.
/src
/.git
/scripts
.gitignore
.env
tsconfig.tsbuildinfo